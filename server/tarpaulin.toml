[default]
# Exclude files that require external services or are entry-point only
exclude-files = [
    "server/src/main.rs",   # binary entry point — not unit testable
    "server/src/chain.rs",  # Pimlico JSON-RPC — requires live bundler, tested via E2E
]
